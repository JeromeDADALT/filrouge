{% extends 'admin/common/base.html.twig' %}

{% block structure %}

    {# pour afficher le formulaire j'utilise form() en passant la variable qui contient le formulaire #}
    {{ form(formStructure) }}

{% endblock %}